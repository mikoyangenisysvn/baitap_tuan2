# Tạo một mảng
my_array = [10, 20, 30, 40, 50]

# Truy cập phần tử theo chỉ số và in ra giá trị
index = 2  # Chỉ số của phần tử cần truy cập
element_at_index = my_array[index]
print(f"Element at index {index}: {element_at_index}")
