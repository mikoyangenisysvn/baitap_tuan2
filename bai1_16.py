# Tạo mảng từ 0 đến 4 bằng cách sử dụng range
my_array = list(range(5))

# Hoặc sử dụng comprehension để tạo mảng
my_array = [x for x in range(5)]

# Duyệt qua mảng và in ra mỗi phần tử
for element in my_array:
    print(element)
